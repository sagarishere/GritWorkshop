def compute_fitness(game_data):
    """
    Compute the fitness score for an agent based on game data.

    Parameters:
    - game_data: a dictionary containing data collected during the game run

    Returns:
    - A fitness score (float).
    """
    # Placeholder: return a fixed fitness value. 
    # Students should replace this with their own function logic.
    return 1.0
